# Backend dependencies only — frontend (Streamlit) has its own
# requirements.txt under frontend/. Do not add streamlit here.
#
# Previously this file was two concatenated, conflicting blocks: a
# `pip freeze`-style block pinning numpy==2.5.2 (does not exist on PyPI —
# `pip install -r requirements.txt` failed immediately on that line) and
# bcrypt==5.0.0, followed by a second unpinned block pinning
# bcrypt==4.0.1. Replaced with the single, verified-working set below —
# confirmed by installing into a clean venv and running the full app
# (server boots, all endpoints exercised, `pytest` suite passes).

fastapi==0.141.1
uvicorn[standard]==0.52.3
sqlalchemy==2.0.52
pydantic==2.13.4
pydantic-settings==2.15.0
python-jose[cryptography]==3.5.0
python-multipart==0.0.32
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-dotenv==1.2.2
email-validator==2.3.0
groq==0.37.1
langgraph==0.6.11
langchain-core==1.5.4
langchain-groq==1.1.3
PyMuPDF==1.28.2
pytesseract==0.3.13
Pillow==12.3.0
pytest==9.1.1
httpx==0.28.1
